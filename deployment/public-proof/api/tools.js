const account={accountId:'NS-2048',name:'Alex Morgan',email:'alex@northstar.demo',plan:'Growth',status:'active',postcode:'10115'};
function argsOf(call){const raw=call?.arguments??call?.parameters??call?.function?.arguments??call?.function?.parameters??{};if(typeof raw==='string'){try{return JSON.parse(raw)}catch{return {}}}return raw||{}}
function run(name,args){if(name==='lookup_account'){const id=String(args.accountId||'').toUpperCase();const email=String(args.email||'').toLowerCase();if(id!==account.accountId&&email!==account.email)return{status:'not_found',message:'No matching demo account. Offer a human handoff.'};return{status:'found',account:{accountId:account.accountId,name:account.name,email:account.email,plan:account.plan,accountStatus:account.status},verification:{method:'postcode'}}}
if(name==='perform_action'){if(String(args.postcode||'')!==account.postcode)return{status:'requires_human',reason:'identity_not_verified',message:'Verification failed.'};const action=String(args.action||'');if(action==='invoice_copy')return{status:'completed',action,reference:'INV-COPY-2048',delivery:'Sent to a•••@northstar.demo'};if(action==='password_reset')return{status:'completed',action,reference:'RESET-2048',delivery:'Reset link sent to a•••@northstar.demo'};if(action==='service_status')return{status:'completed',action,reference:'STATUS-OK',service:'operational',activeIncidents:0};return{status:'requires_human',reason:'action_not_allow_listed',message:'Only low-risk actions are autonomous.'}}
if(name==='escalate_to_human')return{status:'queued',caseId:`LF-${String(Date.now()).slice(-6)}`,contextAttached:true,reason:String(args.reason||'unspecified'),summary:String(args.summary||'').slice(0,500)};return{status:'error',reason:'unknown_tool'}}
function calls(payload){const m=payload?.message||{};if(Array.isArray(m.toolCallList))return m.toolCallList;if(Array.isArray(m.toolWithToolCallList))return m.toolWithToolCallList.map(x=>x?.toolCall||x).filter(Boolean);if(payload?.name)return[payload];return[]}
export default async function handler(req,res){if(req.method==='GET')return res.status(200).json({ok:true,service:'launchforge-public-tools',tools:['lookup_account','perform_action','escalate_to_human']});if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});const list=calls(req.body||{});if(!list.length)return res.status(400).json({error:'no_tool_calls'});return res.status(200).json({results:list.map(call=>{const name=String(call.name||call.function?.name||'unknown');return{name,toolCallId:call.id||`local-${name}`,result:JSON.stringify(run(name,argsOf(call)))}})})}
